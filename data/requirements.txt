# Production dependencies
meshtastic>=2.7.11
meshcore>=2.3.8
bleak>=3.0.2
protobuf>=7.35.1
cryptography>=49.0.0  # AES-CTR decryption for the passive UDP transport
rns>=1.4.2  # Reticulum announce listener (PROTOCOL=reticulum)

# Development dependencies (optional)
black>=26.5.1
pytest>=9.1.1
pytest-cov>=7.1.0
